---
"openapi-chain": minor
---

Resolve strict fluent routes with one trailing slash from compiled metadata, preserving exact URLs and per-method selection. Same-method route collisions now require the typed `$path()` escape in both strict types and runtime. Core routing remains unchanged.
