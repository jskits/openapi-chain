---
"openapi-chain": patch
"@openapi-chain/cli": patch
"@openapi-chain/query": patch
---

Organize the repository as a pnpm monorepo with Turbo builds and independent packed-consumer checks. Keep the runtime published as `openapi-chain` and publish the CLI and query adapter as `@openapi-chain/cli` and `@openapi-chain/query`.
