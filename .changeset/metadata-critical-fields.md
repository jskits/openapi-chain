---
"openapi-chain": patch
---

Reject malformed required flags, missing request body content, non-object media entries, and invalid Encoding header maps instead of silently weakening serialization metadata.
