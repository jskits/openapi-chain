---
"openapi-chain": patch
---

Share URL joining and query suffix handling across core and strict. Avoid duplicate query separators and normalize trailing base URL slashes consistently without altering route path fidelity.
