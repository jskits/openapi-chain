---
'openapi-chain': minor
---

Add exact-path selection to metadata compilation. Resolve references against the full source document, reject unknown selections, and retain fail-closed completeness for the selected subset.
