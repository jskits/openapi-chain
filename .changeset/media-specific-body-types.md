---
'openapi-chain': patch
---

Type request bodies from the most specific matching media declaration, matching runtime selection, so a wildcard declaration can no longer supply the body type for a more specific declared media type.
