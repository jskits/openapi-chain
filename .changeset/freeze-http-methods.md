---
"openapi-chain": patch
---

Freeze the exported HTTP method tuple at runtime so JavaScript consumers cannot change global Proxy dispatch, route indexing or metadata compilation. Preserve the public readonly tuple API.
