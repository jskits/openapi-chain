---
"openapi-chain-cli": minor
---

Add the official openapi-chain command for deterministic build-time generation of TypeScript declarations, scoped types, runtime metadata and a provenance manifest. Support local OpenAPI 3.0/3.1 JSON/YAML, exact path selection and read-only drift checks, with protected output directories and isolated Node tool dependencies.
