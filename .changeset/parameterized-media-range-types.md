---
"openapi-chain": patch
---

Require explicit concrete content types for parameterized media ranges and accept concrete selections with their declared parameter suffix. Keep wildcards in parameter values separate from wildcards in the media type itself.
